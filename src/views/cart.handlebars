<h1>Carrito de Compras</h1>

{{#if cart.selectedProduct.length}}
  <ul>
    {{#each cart.selectedProduct}}
      <li>
        <h2>{{product.title}}</h2>
        <p>Precio: ${{product.price}}</p>
        <p>Cantidad: {{quantity}}</p>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No hay productos en este carrito.</p>
{{/if}}